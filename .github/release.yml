name: release
on: 
    push:
        branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps: 
     - name: Checkout 
     uses: actions/checkout@v6.0.2
     - name: Flutter setup
     uses: subosito/flutter-action@v2
       with:
        channel: stable
        run: flutter --version
     # Por ahora que haga estas dos cosas mientras programo el sript bash para ejecutar las builds
      
